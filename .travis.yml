cache:
  directories:
    - node_modiles
script:
  - npm run build
branches:
  only:
    - master
after_script:
  - docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD"
  - docker push giangbuiquang/aatest